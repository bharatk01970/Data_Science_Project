def _capture_single_screenshot(self, workbook, sheet_name, cell_range, name, folder, embed=True, attach=False):
    """Helper method to capture a single screenshot with reliable clipboard handling"""
    try:
        self.log_message(f"  Capturing {name} from {sheet_name}!{cell_range}", 'info')
        sheet = workbook.Sheets(sheet_name)
        range_obj = sheet.Range(cell_range)
        range_obj.CopyPicture(Format=2)
        workbook.Windows(1).Activate()
        time.sleep(0.7)

        # Wait until image appears in clipboard (retry up to ~2 seconds)
        img = None
        for _ in range(10):
            img = ImageGrab.grabclipboard()
            if img:
                break
            time.sleep(0.2)

        if img is None:
            self.log_message(f"  Failed to capture {name} (clipboard empty)", 'error')
            return

        save_path = os.path.join(folder, f"{name}.png")
        img.save(save_path, 'PNG')

        screenshot_info = {
            'path': save_path,
            'name': name,
            'embed': embed,
            'attach': attach
        }
        self.screenshot_paths.append(screenshot_info)
        self.log_message(f"  ✓ Saved {name}", 'success')
        time.sleep(0.3)  # brief delay to avoid clipboard overwrite

    except Exception as e:
        self.log_message(f"  Error capturing {name}: {str(e)}", 'error')