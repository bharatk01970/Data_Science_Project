def detect_files_in_folder(self):
    """Detect which configured files exist in the watch folder"""
    import fnmatch
    
    detected_files = []
    
    # ... folder validation code (UNCHANGED) ...
    
    for file_key, config in self.file_configs.items():
        pattern = config.get('file_pattern', '')
        
        if not pattern:
            print(f"WARNING: {file_key}: No file_pattern configured, skipping")
            continue
        
        matched_files = fnmatch.filter(folder_files, pattern)
        
        if matched_files:
            # NEW: Process ALL matched files, not just the first one
            print(f"✓ Detected {len(matched_files)} file(s) for {file_key} pattern '{pattern}':")
            
            for idx, matched_file in enumerate(matched_files, 1):  # ← LOOP ALL FILES
                file_path = os.path.join(self.watch_folder, matched_file)
                
                detected_files.append({
                    'file_key': f"{file_key}_{idx}" if len(matched_files) > 1 else file_key,  # ← UNIQUE KEY
                    'original_file_key': file_key,  # ← NEW FIELD
                    'file_path': file_path,
                    'file_name': matched_file,
                    'config': config
                })
                
                print(f"  {idx}. {matched_file}")  # ← NUMBERED LIST







####



def process_next_file(self):
    """Process the next file in the queue"""
    if self.current_file_index >= len(self.files_to_process):
        # ... completion code (UNCHANGED) ...
        return
    
    file_info = self.files_to_process[self.current_file_index]
    file_key = file_info['file_key']
    original_file_key = file_info.get('original_file_key', file_key)  # ← NEW LINE
    file_path = file_info['file_path']
    file_name = file_info['file_name']
    file_config = file_info['config'].copy()
    
    # ... rest of function ...
    
    # Create file-specific logger with actual filename
    file_logger = self.setup_file_specific_logging(original_file_key, file_name)  # ← USES ORIGINAL KEY