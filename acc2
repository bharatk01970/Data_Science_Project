Option Explicit

' ===================================================================
' TM1 Instance Health Check Validator
' Purpose: Automate validation of TM1 instances in Citrix environment
' ===================================================================

' Configuration Constants
Const TIMEOUT_SECONDS As Long = 30
Const LOG_SHEET_NAME As String = "TM1_ValidationLog"
Const CONFIG_SHEET_NAME As String = "TM1_Instances"

' TM1 API Object References
Dim tm1API As Object
Dim tm1Server As Object

Sub ValidateAllTM1Instances()
    ' Main procedure to validate all configured TM1 instances
    
    Dim wsConfig As Worksheet
    Dim wsLog As Worksheet
    Dim lastRow As Long
    Dim i As Long
    Dim instanceName As String
    Dim serverAddress As String
    Dim startTime As Double
    Dim validationResult As String
    Dim errorMessage As String
    Dim isHealthy As Boolean
    
    On Error GoTo ErrorHandler
    
    ' Initialize worksheets
    Set wsConfig = GetOrCreateSheet(CONFIG_SHEET_NAME)
    Set wsLog = GetOrCreateSheet(LOG_SHEET_NAME)
    
    ' Setup log sheet headers if needed
    Call InitializeLogSheet(wsLog)
    
    ' Validate configuration sheet structure
    If Not ValidateConfigSheet(wsConfig) Then
        MsgBox "Configuration sheet is invalid. Please ensure columns: Instance Name, Server Address", vbCritical
        Exit Sub
    End If
    
    ' Get last row of instance list
    lastRow = wsConfig.Cells(wsConfig.Rows.Count, 1).End(xlUp).Row
    
    If lastRow < 2 Then
        MsgBox "No TM1 instances configured. Please add instances to the " & CONFIG_SHEET_NAME & " sheet.", vbInformation
        Exit Sub
    End If
    
    ' Display progress
    Application.ScreenUpdating = False
    Application.StatusBar = "Starting TM1 instance validation..."
    
    ' Initialize TM1 API
    On Error Resume Next
    Set tm1API = CreateObject("TM1API.TM1API")
    If tm1API Is Nothing Then
        MsgBox "Failed to initialize TM1 API. Please ensure TM1 is properly installed.", vbCritical
        Exit Sub
    End If
    On Error GoTo ErrorHandler
    
    ' Iterate through all instances
    For i = 2 To lastRow
        instanceName = Trim(wsConfig.Cells(i, 1).Value)
        serverAddress = Trim(wsConfig.Cells(i, 2).Value)
        
        If instanceName <> "" And serverAddress <> "" Then
            Application.StatusBar = "Validating instance " & i - 1 & " of " & lastRow - 1 & ": " & instanceName
            
            startTime = Timer
            isHealthy = False
            errorMessage = ""
            
            ' Validate the instance
            isHealthy = ValidateTM1Instance(instanceName, serverAddress, errorMessage)
            
            ' Log the result
            Call LogValidationResult(wsLog, instanceName, serverAddress, isHealthy, errorMessage)
            
            ' Brief pause between checks
            Application.Wait Now + TimeValue("00:00:01")
        End If
    Next i
    
    ' Cleanup
    Call CleanupTM1Objects
    
    Application.ScreenUpdating = True
    Application.StatusBar = False
    
    MsgBox "TM1 instance validation completed. Check the " & LOG_SHEET_NAME & " sheet for results.", vbInformation
    
    Exit Sub
    
ErrorHandler:
    Application.ScreenUpdating = True
    Application.StatusBar = False
    Call CleanupTM1Objects
    MsgBox "Error in validation process: " & Err.Description, vbCritical
End Sub

Function ValidateTM1Instance(instanceName As String, serverAddress As String, ByRef errorMsg As String) As Boolean
    ' Validates a single TM1 instance by attempting to connect and verify components
    
    Dim startTime As Double
    Dim isConnected As Boolean
    Dim tm1Process As Object
    Dim cubeCount As Long
    
    On Error GoTo InstanceError
    
    ValidateTM1Instance = False
    errorMsg = ""
    startTime = Timer
    
    ' Attempt to connect to TM1 server
    Set tm1Server = tm1API.Connect(serverAddress)
    
    If tm1Server Is Nothing Then
        errorMsg = "Failed to connect to server"
        Exit Function
    End If
    
    ' Check timeout
    If (Timer - startTime) > TIMEOUT_SECONDS Then
        errorMsg = "Connection timeout exceeded"
        Call DisconnectTM1
        Exit Function
    End If
    
    ' Verify server connection is active
    On Error Resume Next
    cubeCount = tm1Server.Cubes.Count
    If Err.Number <> 0 Then
        errorMsg = "Server connection failed: " & Err.Description
        Err.Clear
        Call DisconnectTM1
        Exit Function
    End If
    On Error GoTo InstanceError
    
    ' Additional component checks
    If Not ValidateTM1Components(errorMsg) Then
        Call DisconnectTM1
        Exit Function
    End If
    
    ' Check total validation time
    If (Timer - startTime) > TIMEOUT_SECONDS Then
        errorMsg = "Component validation timeout"
        Call DisconnectTM1
        Exit Function
    End If
    
    ' Successful validation
    ValidateTM1Instance = True
    errorMsg = "Instance healthy - All components loaded"
    
    Call DisconnectTM1
    Exit Function
    
InstanceError:
    errorMsg = "Error: " & Err.Description & " (Code: " & Err.Number & ")"
    ValidateTM1Instance = False
    Call DisconnectTM1
End Function

Function ValidateTM1Components(ByRef errorMsg As String) As Boolean
    ' Validates critical TM1 components are accessible
    
    Dim dimCount As Long
    Dim processCount As Long
    
    On Error GoTo ComponentError
    
    ValidateTM1Components = False
    
    ' Check Dimensions
    dimCount = tm1Server.Dimensions.Count
    If dimCount < 0 Then
        errorMsg = "Dimensions collection not accessible"
        Exit Function
    End If
    
    ' Check Cubes
    If tm1Server.Cubes.Count < 0 Then
        errorMsg = "Cubes collection not accessible"
        Exit Function
    End If
    
    ' Check Processes
    On Error Resume Next
    processCount = tm1Server.Processes.Count
    If Err.Number <> 0 Then
        errorMsg = "Processes collection not accessible"
        Err.Clear
        Exit Function
    End If
    On Error GoTo ComponentError
    
    ValidateTM1Components = True
    Exit Function
    
ComponentError:
    errorMsg = "Component validation error: " & Err.Description
    ValidateTM1Components = False
End Function

Sub DisconnectTM1()
    ' Safely disconnect from TM1 server
    On Error Resume Next
    If Not tm1Server Is Nothing Then
        tm1Server.Disconnect
        Set tm1Server = Nothing
    End If
    On Error GoTo 0
End Sub

Sub CleanupTM1Objects()
    ' Release all TM1 object references
    On Error Resume Next
    Call DisconnectTM1
    Set tm1API = Nothing
    On Error GoTo 0
End Sub

Function GetOrCreateSheet(sheetName As String) As Worksheet
    ' Gets existing sheet or creates new one
    Dim ws As Worksheet
    
    On Error Resume Next
    Set ws = ThisWorkbook.Worksheets(sheetName)
    On Error GoTo 0
    
    If ws Is Nothing Then
        Set ws = ThisWorkbook.Worksheets.Add(After:=ThisWorkbook.Worksheets(ThisWorkbook.Worksheets.Count))
        ws.Name = sheetName
    End If
    
    Set GetOrCreateSheet = ws
End Function

Sub InitializeLogSheet(ws As Worksheet)
    ' Sets up the log sheet with headers
    
    If ws.Cells(1, 1).Value = "" Then
        ws.Cells(1, 1).Value = "Timestamp"
        ws.Cells(1, 2).Value = "Instance Name"
        ws.Cells(1, 3).Value = "Server Address"
        ws.Cells(1, 4).Value = "Status"
        ws.Cells(1, 5).Value = "Message"
        
        ' Format headers
        With ws.Range("A1:E1")
            .Font.Bold = True
            .Interior.Color = RGB(79, 129, 189)
            .Font.Color = RGB(255, 255, 255)
            .HorizontalAlignment = xlCenter
        End With
        
        ws.Columns("A:E").AutoFit
    End If
End Sub

Function ValidateConfigSheet(ws As Worksheet) As Boolean
    ' Validates the configuration sheet has required columns
    ValidateConfigSheet = False
    
    If ws.Cells(1, 1).Value = "" Then
        ' Setup config sheet if empty
        ws.Cells(1, 1).Value = "Instance Name"
        ws.Cells(1, 2).Value = "Server Address"
        
        With ws.Range("A1:B1")
            .Font.Bold = True
            .Interior.Color = RGB(79, 129, 189)
            .Font.Color = RGB(255, 255, 255)
        End With
        
        ' Add sample data
        ws.Cells(2, 1).Value = "PROD_Instance"
        ws.Cells(2, 2).Value = "localhost:12345"
        
        ws.Columns("A:B").AutoFit
    End If
    
    ValidateConfigSheet = True
End Function

Sub LogValidationResult(ws As Worksheet, instanceName As String, serverAddress As String, isHealthy As Boolean, errorMsg As String)
    ' Logs validation result to the log sheet
    Dim nextRow As Long
    Dim statusText As String
    Dim statusColor As Long
    
    nextRow = ws.Cells(ws.Rows.Count, 1).End(xlUp).Row + 1
    
    ' Determine status
    If isHealthy Then
        statusText = "AVAILABLE"
        statusColor = RGB(0, 176, 80)
    Else
        statusText = "UNAVAILABLE"
        statusColor = RGB(255, 0, 0)
    End If
    
    ' Write log entry
    ws.Cells(nextRow, 1).Value = Now
    ws.Cells(nextRow, 2).Value = instanceName
    ws.Cells(nextRow, 3).Value = serverAddress
    ws.Cells(nextRow, 4).Value = statusText
    ws.Cells(nextRow, 5).Value = errorMsg
    
    ' Format status cell
    With ws.Cells(nextRow, 4)
        .Font.Bold = True
        .Font.Color = statusColor
    End With
    
    ' Format timestamp
    ws.Cells(nextRow, 1).NumberFormat = "yyyy-mm-dd hh:mm:ss"
    
    ws.Columns("A:E").AutoFit
End Sub

Sub ClearValidationLog()
    ' Utility to clear the validation log (keeps headers)
    Dim ws As Worksheet
    Dim lastRow As Long
    
    On Error Resume Next
    Set ws = ThisWorkbook.Worksheets(LOG_SHEET_NAME)
    On Error GoTo 0
    
    If Not ws Is Nothing Then
        lastRow = ws.Cells(ws.Rows.Count, 1).End(xlUp).Row
        If lastRow > 1 Then
            ws.Range("A2:E" & lastRow).ClearContents
            MsgBox "Validation log cleared.", vbInformation
        Else
            MsgBox "No log entries to clear.", vbInformation
        End If
    Else
        MsgBox "Log sheet not found.", vbExclamation
    End If
End Sub